import { Layout } from "@components";
import { PostPreview } from "components/PostPreview";

const Home = () => (
  <Layout>
    <PostPreview />
    <PostPreview />
  </Layout>
);

export default Home;
